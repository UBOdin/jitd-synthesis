@import jitd.codegen._
@(ctx:Render)

typedef enum { @for(node <- ctx.definition.nodeTypes) {
  @node.enumName, }
} JITDNodeType;

class JITDNode {
  public: 
  JITDNode(JITDNodeType type) : type(type) {}

  JITDNodeType type;
};