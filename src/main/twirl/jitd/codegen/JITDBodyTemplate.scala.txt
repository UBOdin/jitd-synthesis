@import jitd.codegen._
@import jitd.spec._
@(definition: Definition, ctx:Render)

@for( (name, accessor) <- definition.accessors ) {
  
  @{RenderAccessor.declaration("JITD::"+name, accessor, ctx)} {
    @{RenderAccessor.body(name, accessor, ctx)}
  }


}